# GitHub 项目解读 Agent

一个运行在 Windows 桌面的轻量级 AI Agent：粘贴 GitHub 仓库链接，自动获取 README，并调用你自己配置的大模型 API，生成结构化中文解读、完整中文翻译和“人话版”通俗理解。

## 功能

- GitHub 仓库链接自动识别，无需手动复制 README
- 通过 GitHub API 获取 README，稳定且不依赖网页抓取
- 支持任意 OpenAI 兼容的 LLM API（DeepSeek、OpenAI、通义、智谱等）
- 输出：项目名称、项目是做什么的、解决了什么问题、技术栈、核心功能、工作流程、通俗理解、README 中文翻译
- 输出顺序：先给出约 1000 字的 README 内容总结，再完整翻译 README 原文
- 流式输出时不会自动滚动，滚动位置完全由你手动控制
- 长 README 自动分段理解，避免超出模型上下文限制
- 流式输出，分析过程实时可见，可随时停止

## 运行

1. 双击 `run.bat`（首次运行会自动创建虚拟环境并安装依赖）。
2. 首次打开后点击右上角 `⚙`，填写你的 LLM API 地址、API Key 和模型名称。
3. 粘贴 GitHub 仓库链接，点击“开始分析”。

配置保存在程序目录下的 `.env` 文件中，不会上传。

## 打包为 exe

双击 `build.bat`，完成后生成 `dist\GitHubReadmeAgent.exe`。可以直接双击运行，也可发送到桌面创建快捷方式。

## 技术栈

Python、PySide6、GitHub API、OpenAI 兼容 LLM API、Pydantic、python-dotenv、PyInstaller。
